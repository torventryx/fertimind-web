const { onRequest } = require('firebase-functions/v2/https');
  const server = import('firebase-frameworks');
  exports.ssrfivmindc7897 = onRequest({"region":"europe-west1","minInstances":0}, (req, res) => server.then(it => it.handle(req, res)));
  